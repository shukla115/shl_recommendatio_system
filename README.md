# shl_recommendatio_system
A Generative AI-powered recommendation system designed to automatically suggest the most suitable SHL assessments based on a given job description or skill-related query.  At its core, the system leverages SentenceTransformer embeddings to convert both job descriptions and SHL assessment data into high-dimensional numerical representations. 
